<!--collect.wxml-->
<view class="main">
  <Head page="collection" tabButton="{{tabButton}}" num="{{num}}" imgs="{{imgs}}" imgCounts="{{imgCounts}}"></Head>
</view>
<ScrollTop style="display:{{isHidden}}"></ScrollTop>
